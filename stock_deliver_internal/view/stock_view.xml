<?xml version="1.0" encoding="UTF-8"?>
<openerp>
	<data>
     	<record id="action_int_picking_move" model="ir.actions.act_window">
            <field name="name">Internal Moves</field>
            <field name="res_model">stock.move</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">['|','&amp;',('picking_id','=',False),('location_dest_id.usage', 'in', ['internal']),'&amp;',('picking_id','!=',False),('picking_id.type','=','internal')]</field>
            <field name="view_id" ref="stock.view_move_tree_reception_picking"/>
            <field name="context" eval="'{\'search_default_receive\':1}"/>
            <field name="search_view_id" ref="stock.view_move_search_reception_outcoming_picking"/>
            <field name="help">You will find in this list all products you have to deliver to internal locations. You can process the deliveries directly from this list using the buttons on the right of each line. You can filter the products to deliver by customer, products or sale order (using the Origin field).</field>
        </record>
        
        <menuitem action="action_int_picking_move" id="menu_action_pdct_int" parent="stock.menu_stock_products_moves" sequence="3"/>
        
	</data>
</openerp>
